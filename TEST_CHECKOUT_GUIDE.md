# 🧪 Тест Checkout - YooKassa + Яндекс Доставка

## 🚀 Быстрый старт

### 1. Открой браузер
```
http://localhost:3001
```

### 2. Добавь тестовый товар в корзину

Открой консоль браузера (F12) и выполни:

```javascript
// Добавляем тестовый товар в корзину
localStorage.setItem('cart', JSON.stringify([
  {
    productId: 'test-product-1',
    quantity: 2,
    price: 2500
  }
]));

// Обновляем страницу
location.reload();
```

### 3. Перейди в корзину
```
http://localhost:3001/cart
```

### 4. Нажми "Оформить заказ"

---

## 📝 Заполнение формы checkout

### Данные получателя
- **Имя:** Иван
- **Фамилия:** Иванов
- **Телефон:** +7 999 123 45 67
- **Email:** test@example.com
- **Город:** Москва (для виджета Яндекс)

### Способ доставки
Выбери: **Яндекс Доставка (ПВЗ)**

После выбора появится **виджет карты с ПВЗ** ⬇️

---

## 🗺️ Тест виджета Яндекс Доставки

### Что должно произойти:
1. ✅ Появляется карта с пунктами выдачи
2. ✅ На карте видны маркеры ПВЗ
3. ✅ Можно кликнуть на маркер
4. ✅ Показывается информация о ПВЗ:
   - Адрес
   - График работы
   - Стоимость доставки
   - Срок доставки
5. ✅ Есть кнопка "Выбрать"
6. ✅ После выбора появляется зеленое уведомление
7. ✅ Стоимость доставки обновляется в итогах заказа

### Параметры виджета:
- **Город:** Москва
- **Станция отгрузки:** 05e809bb-4521-42d9-a936-0fb0744c0fb3
- **Вес:** 10000г (10 кг)
- **Токен API:** настроен ✓

---

## 💳 Тест оплаты YooKassa

### Способ оплаты
Выбери любой метод оплаты (Card, Cash, Online Payment)

### Нажми "Подтвердить заказ"

### Что должно произойти:
1. ✅ Заказ создается в БД
2. ✅ Сохраняются данные ПВЗ Яндекс:
   - pickup_point_id
   - pickup_point_address
   - delivery_price
   - delivery_term
   - координаты ПВЗ
3. ✅ Перенаправление на страницу оплаты YooKassa
4. ✅ Тестовая форма оплаты YooKassa

### Тестовые карты YooKassa:
- **Успешная оплата:** 
  - Номер: `5555 5555 5555 4477`
  - Срок: `12/24`
  - CVC: `123`

- **Отклоненная оплата:**
  - Номер: `5555 5555 5555 4444`
  - Срок: `12/24`
  - CVC: `123`

---

## 🔍 Проверка данных в БД

После оформления заказа проверь БД:

```bash
docker-compose exec db psql -U postgres -d orderdb
```

```sql
-- Последний заказ
SELECT 
  id,
  user_id,
  total_amount,
  status,
  yandex_pickup_point_id,
  yandex_pickup_point_address,
  yandex_delivery_price,
  yandex_delivery_term,
  created_at
FROM orders 
ORDER BY created_at DESC 
LIMIT 1;
```

### Должны быть заполнены:
- ✅ `yandex_pickup_point_id` - ID ПВЗ
- ✅ `yandex_pickup_point_address` - адрес ПВЗ  
- ✅ `yandex_delivery_price` - стоимость
- ✅ `yandex_delivery_term` - срок в днях
- ✅ `yandex_latitude` / `yandex_longitude` - координаты
- ✅ `yandex_work_schedule` - график работы

---

## 🎯 API endpoints для проверки

### Конфигурация Яндекс Доставки
```bash
curl http://localhost:8087/api/yandex-delivery/config
```

**Ожидается:**
```json
{
  "sourcePlatformStation": "05e809bb-4521-42d9-a936-0fb0744c0fb3",
  "defaultWeight": 10000,
  "apiAvailable": true
}
```

### Методы доставки
```bash
curl http://localhost:8087/api/delivery-methods
```

**Должен быть:** "Яндекс Доставка (ПВЗ)"

### Методы оплаты
```bash
curl http://localhost:8087/api/payment-methods
```

---

## ❗ Если что-то не работает

### Виджет не загружается
1. Открой консоль браузера (F12)
2. Проверь ошибки в Network
3. Убедись что выбран метод "Яндекс Доставка (ПВЗ)"
4. Проверь что скрипт загружается: `https://yastatic.net/s3/delivery-front/widgets/ya-delivery.js`

### Нет товаров в корзине
Выполни в консоли браузера:
```javascript
localStorage.setItem('cart', JSON.stringify([
  { productId: 'test-1', quantity: 1, price: 2500 },
  { productId: 'test-2', quantity: 2, price: 1500 }
]));
location.reload();
```

### Ошибка при создании заказа
Проверь логи:
```bash
docker-compose logs -f orderservice
```

### YooKassa не работает
Проверь конфигурацию:
```bash
docker-compose exec orderservice env | grep YOOKASSA
```

---

## ✅ Чеклист проверки

- [ ] Фронтенд открывается (localhost:3001)
- [ ] Товар добавлен в корзину
- [ ] Переход на checkout работает
- [ ] Форма заполнена
- [ ] Методы доставки загрузились
- [ ] Есть "Яндекс Доставка (ПВЗ)"
- [ ] При выборе появляется виджет карты
- [ ] На карте видны ПВЗ
- [ ] ПВЗ выбирается
- [ ] Появляется зеленое уведомление
- [ ] Стоимость доставки отображается
- [ ] Методы оплаты загрузились
- [ ] Кнопка "Подтвердить заказ" активна
- [ ] Заказ создается
- [ ] Данные сохраняются в БД
- [ ] Перенаправление на YooKassa
- [ ] Форма оплаты открывается

---

## 🎉 Успех!

Если все работает:
- ✅ Яндекс Доставка интегрирована
- ✅ Виджет ПВЗ работает
- ✅ Данные сохраняются
- ✅ YooKassa настроена
- ✅ Заказы создаются

**Готово к использованию!** 🚀
